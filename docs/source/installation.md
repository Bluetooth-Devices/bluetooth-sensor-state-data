# Installation

The package is published on [PyPI](https://pypi.org/project/bluetooth-sensor-state-data/) and can be installed with `pip` (or any equivalent):

```bash
pip install bluetooth-sensor-state-data
```
